---
import { practice } from "../lib/practice";
import { appointmentHref } from "../lib/features";
---

<section id="contact" class="px-4 py-14 sm:py-16">
  <div class="reveal reveal-zoom mx-auto grid max-w-7xl items-center gap-8 rounded-4xl bg-navy p-6 text-white sm:rounded-6xl sm:p-8 md:p-14 lg:grid-cols-[1.2fr_.8fr]">
    <div>
      <p class="text-xs font-bold tracking-[.22em] text-air uppercase sm:text-sm">Schedule with Eastland</p>
      <h2 class="mt-4 font-heading text-4xl leading-tight md:text-6xl">A calmer path back to the dentist starts here.</h2>
      <p class="mt-5 text-base text-white/70 sm:text-lg">Call us or visit the contact page to get started. Our scheduling coordinator will help you find and confirm a time that works for you.</p>
    </div>
    <div class="rounded-4xl bg-white p-6 text-navy sm:rounded-5xl">
      <a href={appointmentHref} class="block rounded-full bg-blue py-4 text-center font-bold text-white">Book Appointment</a>
      <a href={practice.phoneHref} class="mt-3 block rounded-full bg-air py-4 text-center font-bold text-navy">{practice.phoneDisplay}</a>
      <p class="mt-6 text-body">Monday–Thursday, 7:00 AM–4:30 PM<br />Friday–Sunday closed</p>
    </div>
  </div>
</section>
